[pytest]
testpaths = tests
python_files = test_*.py
addopts = -v -ra -q
markers =
    asyncio: mark test as async/await test

# 忽略特定警告
filterwarnings =
    ignore::DeprecationWarning:google.auth._helpers